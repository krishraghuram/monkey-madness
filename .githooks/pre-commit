#!/bin/sh

# Pre-commit hook to run npm check
echo "Running pre-commit checks..."

# Run npm check
npm run check

# If npm check fails, prevent commit
if [ $? -ne 0 ]; then
    echo "Pre-commit checks failed. Commit aborted."
    exit 1
fi

echo "Pre-commit checks passed!"
exit 0